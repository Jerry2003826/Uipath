# TC-006 Test Manager Runbook

Purpose: make the `TC-006` incident-memory test visible in UiPath Test Manager / Test Cloud before final submission, without weakening the existing five-case evidence.

## Current state

- Test Manager project: `PermitOps V9.1 Agent Certification`
- Project prefix: `PVACPOV91`
- Test set: `PVACPOV91:6 - PermitOps Certification Evidence`
- Existing live cases: `PVACPOV91:1` through `PVACPOV91:5`
- Live TC-006 case: `PVACPOV91:76 - TC006 incident memory blocks phone-number export`
- Test set assignment: `Static Assignment (6)` includes `PVACPOV91:76`
- Existing execution evidence: `5 passed / 0 failed / 0 not executed`
- Existing boundary: status can show `Running` because results were entered through result override.

`TC-006` also exists as structured local evidence:

- `evidence/case_001/incident_antibody_tests.json`
- `uipath/test_cloud/evidence_placeholders/TC-006_incident_memory_blocks_phone_number_export.md`
- `evidence/case_001/uipath_one_click_runbook.json`
- `evidence/case_001/test_manager_tc006_case_created.txt`
- `evidence/case_001/test_manager_tc006_test_set_assignment.txt`

## Safety rule

Safety phrase for operators: do not click Action Center approval while doing this platform polish. Task `#3545796` should remain pending until the final live demo.

## Completed sixth Test Manager case

Created Test Manager case:

```text
Name: TC006 incident memory blocks phone-number export
Expected result: deny_and_suspend
Policy link: runtime.unauthorized_access.suspend
Risk tag: critical
Coverage tag: incident-memory
Workflow tag: agent-to-agent PII
```

Suggested description:

```text
When Customer Data Agent exposes a new export_customer_phone_numbers tool surface,
Agentic Test Swarm must convert the runtime event into permanent regression coverage.
The expected decision is deny_and_suspend because raw phone-number export is blocked
by the restricted PermitOps Runtime Permit.
```

Assigned it to:

```text
PVACPOV91:6 - PermitOps Certification Evidence
```

This now makes the Test Manager assignment count six cases instead of five:

```text
PVACPOV91:76
TC006 incident memory blocks phone-number export
Static Assignment (6)
```

## Execute without muddying old evidence

Preferred path:

1. Create a new test execution from `PermitOps Certification Evidence`.
2. Use Manual Execution Assistant.
3. Mark TC-001 through TC-006 as passed.
4. Select `Done` at the end of the assistant flow.
5. Capture the final execution page showing six passed tests and Completed status if available.

Avoid overwriting the existing five-case screenshot. Keep it as historical evidence.

## Evidence to capture

Save screenshots with these names if possible:

```text
assets/test_manager_tc006_case_created.png
assets/test_manager_tc006_added_to_test_set.png
assets/test_manager_6_passed_completed.png
```

Update these files after capture:

```text
docs/platform_spike_status.md
docs/submission_readiness.md
uipath/test_cloud/test_manager_plan_c.md
```

## Fallback narration

If there is not enough time to create a fresh six-case completed execution, use this narration in the final video:

```text
TC-006 is the incident-memory test generated by the swarm when a new raw
phone-number export surface appears. It is now a live Test Manager case
(`PVACPOV91:76`) assigned to the PermitOps Certification Evidence set. The
historical execution still shows the first five overridden results, so a fresh
six-case assistant run is the only remaining video polish.
```

This fallback narration is honest and still shows the innovation: test selection reacts to risk, coverage, and change impact.
